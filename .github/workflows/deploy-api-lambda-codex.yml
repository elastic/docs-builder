name: Deploy API Lambda (Codex)

on:
  workflow_call: 
    inputs: 
      environment:
        required: true
        type: string
        description: "Codex environment name (e.g. engineering)"
      ref:
        required: false
        type: string
        default: ${{ github.ref }}
    
permissions: 
  contents: read

jobs:
  build:
    permissions: 
      contents: read
    uses: ./.github/workflows/build-api-lambda.yml
    with: 
      ref: ${{ inputs.ref }}
  
  deploy:
    concurrency: ${{ github.workflow }}-codex-${{ inputs.environment }}
    permissions: 
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    needs: build
    environment: codex-api-${{ inputs.environment }}
    env:
      ZIP_FILE: api-lambda.zip
    steps:
      - uses: actions/checkout@v6
          
      - name: Download bootstrap binary
        uses: actions/download-artifact@v7
        with:
          name: api-lambda-binary

      - name: Create zip
        run: |
          zip -j "${ZIP_FILE}" ./bootstrap

      - uses: aws-actions/configure-aws-credentials@61815dcd50bd041e203e49132bacad1fd04d2708 # v5.1.1
        with:
          role-to-assume: arn:aws:iam::197730964718:role/elastic-codex-api-deployer-${{ inputs.environment }}
          aws-region: us-east-1

      - name: Upload to S3 and Deploy Lambda
        run: |
          # Upload once to S3 with git SHA for traceability
          S3_KEY="codex/${GITHUB_SHA}/api-lambda.zip"
          aws s3 cp "${ZIP_FILE}" "s3://${S3_BUCKET}/${S3_KEY}"
          
          # Deploy to streaming-optimized Lambda (codex is stream-only, no API Gateway Lambda)
          aws lambda update-function-code \
            --function-name "${LAMBDA_STREAM_FUNCTION}" \
            --s3-bucket "${S3_BUCKET}" \
            --s3-key "${S3_KEY}"

          # Wait for the function update to complete before publishing
          aws lambda wait function-updated \
            --function-name "${LAMBDA_STREAM_FUNCTION}"
          
          STREAM_VERSION=$(aws lambda publish-version \
            --function-name "${LAMBDA_STREAM_FUNCTION}" \
            --description "Deployed from ${GITHUB_SHA}" \
            --query 'Version' \
            --output text)
          
          aws lambda update-alias \
            --function-name "${LAMBDA_STREAM_FUNCTION}" \
            --name live \
            --function-version $STREAM_VERSION
          
          echo "✅ Deployed to codex Lambda from S3: ${S3_KEY}"
          echo "   ${LAMBDA_STREAM_FUNCTION}: version $STREAM_VERSION (${GITHUB_SHA})"
          
        env:
          S3_BUCKET: elastic-docs-v3-api-lambda-artifacts
          LAMBDA_STREAM_FUNCTION: elastic-codex-${{ inputs.environment }}-lambda-stream-optimized
