# Security settings (fixture stub)

This file exists so Kibana settings YAML samples under `docs/testing/` can resolve the relative link `./security-settings.md` when descriptions are parsed from those YAML files.

## Audit logging settings

Use the generated settings content from `kibana-security-settings.yml` for real documentation.
