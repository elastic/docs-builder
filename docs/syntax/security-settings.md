# Security settings (fixture stub)

This file exists so Kibana settings YAML samples can resolve the relative link `./security-settings.md` when descriptions are parsed from the YAML next to this page.

## Audit logging settings

Use the generated settings content from `kibana-security-settings.yml` for real documentation.
